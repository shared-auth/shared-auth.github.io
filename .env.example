# Leave these values fail-closed until the production authorities are provisioned.
PUBLIC_DASHBOARD_URL=
PUBLIC_DASHBOARD_URL_ALLOWLIST=[]
# Public Cloudflare Turnstile site key. The matching secret key belongs only in
# the API-side secret manager and must never be exposed to this static site.
PUBLIC_TURNSTILE_SITE_KEY=

# Leave these values fail-closed until the production authorities are provisioned.
PUBLIC_DASHBOARD_URL=
PUBLIC_DASHBOARD_URL_ALLOWLIST=[]

# Exact allowed values when the intake API is promoted:
# https://api.ores-shared-auth.com/v1/quote-requests
# https://api.ores-shared-auth.com/v1/pre-interest-registrations
PUBLIC_QUOTE_INTAKE_URL=
PUBLIC_PRE_INTEREST_INTAKE_URL=

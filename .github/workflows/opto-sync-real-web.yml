name: opto-sync real static-site e2e

on:
  pull_request:
    paths:
      - 'index.html'
      - '.github/workflows/opto-sync-real-web.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  real-site:
    uses: opto-sync/opto-sync-e2e/.github/workflows/real-web-app-reusable.yml@baccdb78bf9af696b516e7f7f599c11fdd859387
    with:
      skip_repository_commands: true
